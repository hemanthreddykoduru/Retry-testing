export default function DemoBanner() {
  return (
    <div className="bg-yellow-100 text-yellow-800 p-4 text-center text-sm font-semibold sticky top-0 z-50 shadow-sm border-b border-yellow-200">
      <p>
        🚧 Demo environment — Razorpay Test Mode only. No real money is charged. No sign-up required.
      </p>
    </div>
  );
}
